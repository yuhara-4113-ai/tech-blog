name: Publish articles

on:
  push:
    branches:
      - main
    paths:
      - 'qiita/**'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: publish-articles-main
  cancel-in-progress: false

jobs:
  publish_articles:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: ./.github/actions/qiita-publish
        with:
          qiita-token: ${{ secrets.QIITA_TOKEN }}
          rootdir: ./qiita
